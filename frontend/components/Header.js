import styles from "../styles/Header.module.css"

const Header = () => {
  return (
    <div className={styles.header}>
      <h2 className={styles.title}>Ompractice</h2>
    </div>
  );
};

export default Header;
